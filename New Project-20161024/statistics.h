class statistics {
  
public:
	void resetear();
	int agregar(double valor);
	void analizar();
	double getValor(int idx);
	// constructor
	statistics(int numero,double desde, double hasta);
};
